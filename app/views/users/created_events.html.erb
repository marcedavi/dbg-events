<h1><%= @title %></h1>

<ul>
  <% @events.each do |event| %>
    <li>
      <%= link_to event.name, event %>
      <%= link_to "Participants", event_participations_path(event) %>
    </li>
  <% end %>
</ul>